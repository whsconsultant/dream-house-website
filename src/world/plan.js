/**
 * Legacy heights — prefer floorplan-data.js LEVEL.
 * Kept so older imports do not break during the rewrite.
 */
export const LEVEL = {
  H: 3.6,
  L2: 3.8,
}

export const PLAN = {
  W: 32,
  D: 24,
  terraceDepth: 4,
  glassLineZ: -12,
}
